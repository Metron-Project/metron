[Definition]
# Ban IPs that send requests with a comictagger User-Agent header from nginx.
# The user-agent is the last quoted field in the combined log format.
failregex = (?i)^<HOST> \S+ \S+ \[.*?\] ".*?" \d{3} \d+ ".*?" "comictagger[^"]*"$
ignoreregex =
