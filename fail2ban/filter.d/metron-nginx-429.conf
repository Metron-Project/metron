[Definition]
# Ban IPs that repeatedly trigger HTTP 429 (Too Many Requests) from nginx.
# Matches the standard nginx combined log format logged via journald.
failregex = ^<HOST> \S+ \S+ \[.*?\] ".*?" 429\b
ignoreregex =
