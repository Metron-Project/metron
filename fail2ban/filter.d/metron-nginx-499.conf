[Definition]
# Ban IPs that repeatedly trigger HTTP 499 (Client Closed Request) from nginx.
# 499 indicates the client is closing connections before receiving a response,
# typically caused by flooding requests. Matches the standard nginx combined
# log format.
failregex = ^<HOST> \S+ \S+ \[.*?\] ".*?" 499\b
ignoreregex =
