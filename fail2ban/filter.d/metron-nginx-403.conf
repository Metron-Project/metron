[Definition]
# Ban IPs that repeatedly trigger HTTP 403 (Forbidden) from nginx.
# Matches the standard nginx combined log format logged via journald.
failregex = ^<HOST> \S+ \S+ \[.*?\] ".*?" 403\b
ignoreregex =
