[Definition]
# Ban IPs that repeatedly trigger HTTP 404 (Not Found) from nginx.
# Matches the standard nginx combined log format logged via journald.
failregex = ^<HOST> \S+ \S+ \[.*?\] ".*?" 404\b
ignoreregex =
