[Definition]
# Ban IPs that send requests with no User-Agent header from nginx.
# In the combined log format the user-agent is the last quoted field;
# nginx logs a bare hyphen ("-") when the header is absent.
failregex = ^<HOST> \S+ \S+ \[.*?\] ".*?" \d{3} \d+ ".*?" "-"$
ignoreregex =
