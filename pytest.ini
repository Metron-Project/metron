[pytest]
DJANGO_SETTINGS_MODULE=metron.settings
python_files = tests.py test_*.py
# Run in parallel using all available CPUs
addopts=-n auto -ra --strict-config --strict-markers --cov --cov-report=html --cov-report=term